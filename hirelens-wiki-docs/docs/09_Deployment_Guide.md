# HireLens 2.0 — Deployment Guide

> Full content authored in Sprint 13. Placeholder until then so the file exists and can be linked from the roadmap.

Planned contents: Docker multi-stage build, docker-compose for local prod-parity, CI pipeline (lint/test/build), secrets management (env vars vs. vault), Redis + PgBouncer setup, hosting target decision (Railway/Render/Fly/AWS — TBD based on budget at Sprint 13).
